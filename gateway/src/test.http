### 1. Obtenir le token JWT
POST http://localhost:8083/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin"
}

> {%
    client.test("Token received", function() {
        client.assert(response.status === 200, "Login failed");
        client.assert(response.body.token, "No token in response");
        client.global.set("auth_token", response.body.token);
    });
%}

### 2. Tester la route des notes avec le token
GET http://localhost:8083/api/notes/patient/1
Authorization: Bearer {{auth_token}}

### 3. Tester SANS token (devrait échouer)
GET http://localhost:8083/api/notes/patient/1

### 4. Tester l'accès direct au note-service (si disponible)
GET http://localhost:8081/api/notes/patient/1