spring.application.name=note-service

# MongoDB connection
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=note-db

# Logging (facultatif)
logging.level.org.springframework.data.mongodb.core=DEBUG

server.port=8081
